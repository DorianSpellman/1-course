#include <stdio.h> 
#include <stdlib.h> 
#include <math.h> 

int main()
{
	int x, y;

	printf("Enter x : \n");
	scanf_s("%d", &x);

	printf("Enter y : \n");
	scanf_s("%d", &y);

	if ((x > 0) && (y > 0)) printf ("????? ????? ? I ???????????? ????????");
	if ((x < 0) && (y > 0)) printf ("????? ????? ?? II ???????????? ????????");
	if ((x < 0) && (y < 0)) printf ("????? ????? ? III ???????????? ????????");
	if ((x > 0) && (y < 0)) printf ("????? ????? ? IV ???????????? ????????");

	return 0;
}
